version 18

/*
Step 02: construct Wang-style markup and credit subsidy measures.

Purpose:
  - Replicate the benchmark fact that high-markup sectors receive more
    preferential credit or credit subsidy.
  - Keep own markup as a control and comparison mechanism in later tables.

Planned outputs:
  - firm-level markup_i_t
  - industry-level markup_u_t
  - firm-level credit_subsidy_i_t
  - policy_support_i_u_t, if a broader policy support measure is available

Table mapping:
  - Table 1: Wang-style markup and credit subsidy benchmark.
  - Table 5 and Table 6: credit_subsidy_i_t or policy_support_i_u_t enters as
    treatment/allocation variable, with markup_u_t included as a control.

Important distinction:
  - This step does not construct input-demand wedges.
  - This step does not test supply-chain finance effectiveness.
*/

display as text "Step 02: construct Wang-style markup and credit subsidy measures."
display as text "Status: placeholder. Add measurement code after data paths and variable definitions are fixed."
